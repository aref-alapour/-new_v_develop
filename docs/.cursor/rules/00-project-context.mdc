---
description: Primary project context — main vs reference project; always consider escapezoom_wp when deciding structure, naming, and implementation.
alwaysApply: true
---

# Project context

- **Main project (this workspace):** `escapezoom_wp-dev`. All development and changes happen here.
- **Reference project:** `escapezoom_wp`. Use it to check patterns, structure, conventions, and for ideas when developing the main project.

When deciding architecture, structure, naming, or how to implement something, always consider `escapezoom_wp` as the reference and align with it when relevant.
