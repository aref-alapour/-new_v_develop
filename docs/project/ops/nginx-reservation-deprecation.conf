# LiteSpeed/nginx snippet — reservation.php removed from codebase (Option C).
# Blocks direct browser/mobile hits to the legacy public URL.

location = /web-service/reservation.php {
    return 410;
}

# Optional: allow loopback during transition (remove when EZ_BOOKING_USE_INTERNAL is universal)
# location = /web-service/reservation.php {
#     allow 127.0.0.1;
#     deny all;
#     try_files $uri =404;
#     fastcgi_pass ...;
# }
